# Dependencies for app/notebooks/01_setup_data.ipynb (and shared app.data helpers).
# GRIB read stack: cfgrib needs the ECMWF ecCodes library. If pip install fails,
# install ecCodes via conda-forge (`conda install -c conda-forge eccodes`) then retry.

numpy>=1.24
pandas>=2.0
pyyaml>=6.0
requests>=2.28
folium>=0.15
matplotlib>=3.7
scikit-learn>=1.3
ipython>=8.0
herbie-data
xarray>=2023.7
cfgrib>=0.9.10
metpy>=1.5
